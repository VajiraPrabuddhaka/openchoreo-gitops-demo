apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openchoreo

labels:
  - pairs:
      openchoreo.dev/organization: openchoreo

resources:
  # Release Bindings for all projects
  # These are managed separately to ensure ComponentReleases exist first
  # (OpenChoreo validating webhook requires the referenced ComponentRelease to exist)
  - demo-project-1/
  - ecommerce-demo/